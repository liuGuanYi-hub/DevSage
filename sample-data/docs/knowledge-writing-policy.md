# 知识库写回规则

## 只读与写操作边界

搜索文档、搜索代码和读取文件属于只读操作，可以在用户问题范围内自动执行。

创建或修改 Markdown 知识笔记属于写操作。Agent 只能先生成标题、正文、目标路径和变更预览，必须等用户确认后才能写入 Obsidian 或其他知识库目录。

## 预览内容

写回确认前至少展示：

- 目标文件路径；
- 新建或覆盖操作；
- Markdown 正文；
- 关联来源；
- 如果是更新操作，展示修改前后的差异。

